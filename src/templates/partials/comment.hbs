<div>
    {{#if this.isMine}}
    <div style="display: flex;
    justify-content: space-between;
    width: 30vw;">
        {{this.poster}}.You
    <form action="/post/comment/delete/{{this.id}}?_method=DELETE" method="post" onsubmit="return confirm('Are you sure you want to delete this comment')">
        <button type="submit">Delete</button>
    </form>
    <form action="#" id="{{this.id}}" method="post" onsubmit="editComment(this,event)">
        <button type="submit">edit</button>
    </form>
    </div>
    {{else}}
        {{this.poster}}
    {{/if}}

    {{this.comment}}

<a href="/post/comment/view/{{this.id}}">
Replies:
{{this.repliesCount}}
</a>

</div>

